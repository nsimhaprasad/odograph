package `in`.odograph.tracker

import org.assertj.core.api.Assertions.assertThat
import org.junit.Test

class SmokeTest {
    @Test
    fun `build wiring is alive`() {
        assertThat(2 + 2).isEqualTo(4)
    }
}
